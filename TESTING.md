# ATTN Performance Testing Guide

Lightning-SimulWhisper 기반 개선 사항을 실전 테스트하는 가이드입니다.

## 🎯 테스트 목표

Phase 1 + 2 개선 사항의 실제 성능을 측정하고 검증합니다:

- ✅ **VAD 기반 청킹**: 더 정확한 침묵 감지
- ✅ **컨텍스트 윈도우**: 청크 간 연속성 보장
- ✅ **중복 제거**: Levenshtein 기반 스마트 병합
- ✅ **결과 캐싱**: 재처리 시 90% 시간 절약

---

## 📋 사전 준비

### 1. 테스트 오디오 파일 준비

**권장 테스트 케이스**:

| 파일 유형 | 시간 | 용도 |
|-----------|------|------|
| 짧은 회의 | 5-10분 | 기본 기능 검증 |
| 중간 회의 | 30-45분 | 청킹 품질 확인 |
| 긴 회의 | 1시간+ | 성능 및 캐시 효과 측정 |

**파일 위치**: Obsidian vault 내 임의 위치

### 2. 환경 설정

```bash
# 1. 의존성 확인
npm install

# 2. 빌드
npm run build

# 3. Obsidian에서 플러그인 활성화
# Settings > Community plugins > ATTN 활성화
```

### 3. 벤치마크 디렉토리 확인

자동 생성되는 벤치마크 리포트 위치:
- macOS/Linux: `/tmp/attn-benchmarks/`
- Windows: `%TEMP%\attn-benchmarks\`

---

## 🧪 테스트 시나리오

### 시나리오 1: 기본 기능 검증 (5-10분 파일)

**목적**: Phase 1+2 기능이 올바르게 작동하는지 확인

**절차**:
1. 5-10분 분량의 M4A 파일 선택
2. 파일 우클릭 → "ATTN: Create Summary Note"
3. 처리 완료 대기
4. 생성된 노트 확인

**확인 사항**:
- [ ] 전사 완료 (에러 없음)
- [ ] 노트 생성 성공
- [ ] 콘솔에서 VAD 사용 확인: `"VAD-based silence detection found X intervals"`
- [ ] 컨텍스트 프롬프트 사용 확인: `"Using context prompt: ..."`
- [ ] 중복 제거 확인: `"Detected X overlapping words between chunks"`

**예상 소요 시간**: 2-5분

---

### 시나리오 2: 캐시 효율성 테스트

**목적**: 동일 파일 재처리 시 캐시 작동 확인

**절차**:
1. 10분 분량 파일로 첫 번째 전사 실행
2. 처리 시간 기록 (A)
3. **동일 파일**로 두 번째 전사 실행
4. 처리 시간 기록 (B)
5. 시간 비교: B ≈ 1-2초 (캐시 히트)

**확인 사항**:
- [ ] 첫 번째 실행: 정상 처리
- [ ] 두 번째 실행 콘솔 메시지: `"✓ Cache hit: Skipping transcription for ..."`
- [ ] 두 번째 실행 시간: **90% 이상 감소**
- [ ] 동일한 전사 결과

**예상 결과**:
```
첫 번째 실행: 3분 30초
두 번째 실행: 5초 (94% 감소)
```

---

### 시나리오 3: 청킹 품질 비교 (30-45분 파일)

**목적**: VAD 기반 청킹이 더 정확한지 확인

**절차**:
1. 30분 이상 파일 선택
2. 전사 실행
3. 콘솔 로그에서 다음 확인:
   - Segmentation 로그
   - VAD 감지된 침묵 간격 수
   - 생성된 청크 수

**확인 사항**:
- [ ] VAD 사용: `"VAD-based silence detection found X intervals"`
- [ ] FFmpeg fallback 없음 (VAD 우선 사용)
- [ ] 청크 수가 적절함 (너무 많지 않음)
- [ ] 청크 시간이 자연스러운 대화 구간과 일치

**기대 효과**:
- FFmpeg silencedetect: 불필요한 분할 많음
- VAD 기반: 자연스러운 대화 흐름 유지

---

### 시나리오 4: 컨텍스트 연속성 검증

**목적**: 청크 간 문맥이 유지되는지 확인

**절차**:
1. 전문 용어나 고유명사가 많은 회의 파일 선택
2. 전사 실행
3. 생성된 전사문에서 확인:
   - 인명/회사명 등이 청크 경계에서도 일관되게 전사되는지
   - 문맥이 자연스럽게 이어지는지

**확인 사항**:
- [ ] 콘솔: `"Using context prompt: ..."` 메시지 확인
- [ ] 전사문: 청크 경계에서 문맥 단절 없음
- [ ] 전문 용어: 일관된 철자/표기

**비교 예시**:
```
❌ 이전 (컨텍스트 없음):
청크1: "...그래서 김철수 과장님이..."
청크2: "김철수 부장님이 말씀하셨듯이..." (오류: 직급 불일치)

✅ 개선 (컨텍스트 있음):
청크1: "...그래서 김철수 과장님이..."
청크2: "과장님이 말씀하셨듯이..." (정확: 컨텍스트 유지)
```

---

### 시나리오 5: 중복 제거 효과 측정

**목적**: 청크 병합 시 중복이 제거되는지 확인

**절차**:
1. 2개 이상 청크가 생성되는 파일 선택
2. 전사 실행
3. 콘솔에서 중복 제거 메시지 확인

**확인 사항**:
- [ ] `"Detected X overlapping words between chunks, removing from current chunk"`
- [ ] 전사 결과에 명백한 중복 문장 없음
- [ ] 자연스러운 문장 흐름

---

### 시나리오 6: 장시간 파일 성능 테스트 (1시간+)

**목적**: 대용량 파일에서도 안정적으로 작동하는지 확인

**절차**:
1. 1시간 이상 M4A 파일 선택
2. 전사 실행
3. 처리 시간 및 메모리 사용량 모니터링
4. 벤치마크 리포트 자동 생성 확인

**확인 사항**:
- [ ] 에러 없이 완료
- [ ] 메모리 누수 없음
- [ ] 청크 수가 합리적 범위 (20-40개)
- [ ] 벤치마크 리포트 생성: `/tmp/attn-benchmarks/benchmark-TIMESTAMP.md`

**예상 소요 시간**: 10-20분 (API 속도 따라 다름)

---

## 📊 벤치마크 리포트 읽기

자동 생성되는 마크다운 리포트는 다음을 포함합니다:

### 1. Processing Time Comparison
```markdown
| Metric | Baseline | Improved | Change |
|--------|----------|----------|--------|
| Total Time | 180.5s | 95.2s | 🟢 -47.3% |
```

### 2. Quality Metrics
```markdown
| Metric | Baseline | Improved | Analysis |
|--------|----------|----------|----------|
| Chunk Count | 15 | 12 | 🟢 Better chunking |
| Quality Score | - | 85.3/100 | 🟢 Excellent |
```

### 3. Feature Usage
```markdown
- VAD: ✅ Enabled (32 silence intervals)
- Context Prompts: 11 prompts used
- Deduplication: 47 words removed
- Cache Hit: ❌ No (first run)
```

---

## 🔍 문제 해결

### VAD가 작동하지 않음

**증상**: 콘솔에 `"VAD-based detection failed, falling back to FFmpeg"`

**원인**:
1. ONNX Runtime 설치 안 됨
2. VAD 모델 파일 누락
3. FFmpeg 경로 문제

**해결**:
```bash
# 1. 의존성 재설치
npm install onnxruntime-node

# 2. 모델 파일 확인
ls models/silero_vad.onnx

# 3. 없으면 다시 다운로드
curl -L -o models/silero_vad.onnx \
  "https://github.com/snakers4/silero-vad/raw/master/files/silero_vad.onnx"
```

### 캐시가 작동하지 않음

**증상**: 동일 파일 재처리 시 캐시 히트 안 됨

**원인**:
1. 설정(provider/model/language) 변경
2. 캐시 디렉토리 권한 문제

**확인**:
```bash
# 캐시 파일 확인
ls /tmp/attn-cache/

# 캐시 디렉토리 권한 확인
ls -la /tmp/attn-cache/
```

### 컨텍스트 프롬프트가 사용되지 않음

**증상**: 콘솔에 `"Using context prompt"` 메시지 없음

**원인**: 청크가 1개만 생성됨 (짧은 파일)

**해결**: 더 긴 파일로 테스트 (10분 이상)

---

## 📈 성능 기대치

### 예상 개선 효과

| 파일 길이 | 처리 시간 (이전) | 처리 시간 (개선) | 개선율 |
|-----------|----------------|----------------|-------|
| 10분 | 2분 30초 | 2분 10초 | -13% |
| 30분 | 7분 30초 | 6분 20초 | -15% |
| 1시간 | 15분 | 12분 30초 | -17% |

**캐시 히트 시**: **~2초** (95% 감소)

### 전사 품질 개선

| 지표 | 이전 | 개선 후 |
|------|------|---------|
| 청크 간 중복 | 있음 | 제거됨 |
| 문맥 연속성 | 낮음 | 높음 |
| 전문 용어 일관성 | 낮음 | 높음 |
| 청킹 정확도 | 기준 | +15% |

---

## ✅ 테스트 체크리스트

복사해서 사용하세요:

```markdown
### 기본 기능 테스트
- [ ] 5-10분 파일 전사 성공
- [ ] VAD 작동 확인
- [ ] 컨텍스트 프롬프트 사용 확인
- [ ] 노트 생성 성공

### 캐시 테스트
- [ ] 첫 번째 실행: 정상 처리
- [ ] 두 번째 실행: 캐시 히트 (90% 시간 절감)
- [ ] 동일한 결과 확인

### 품질 테스트
- [ ] 청크 수 적절함
- [ ] 중복 제거 확인
- [ ] 문맥 연속성 확인
- [ ] 전문 용어 일관성 확인

### 성능 테스트
- [ ] 1시간+ 파일 안정적 처리
- [ ] 메모리 누수 없음
- [ ] 벤치마크 리포트 생성 확인
```

---

## 💡 추가 팁

1. **콘솔 로그 활성화**: Developer Tools (Cmd/Ctrl + Shift + I)를 열어 자세한 로그 확인

2. **벤치마크 리포트 비교**: `/tmp/attn-benchmarks/` 디렉토리에서 여러 실행 결과 비교 가능

3. **설정 최적화**:
   - Groq provider: 70배 빠름 (무료 tier 제한 주의)
   - OpenAI: 가장 안정적
   - Gemini: 대용량 파일에 유리

4. **문제 발견 시**: GitHub Issues에 벤치마크 리포트와 함께 제보

---

## 🎓 다음 단계

테스트 완료 후:

1. **벤치마크 리포트 공유**: 커뮤니티와 결과 공유
2. **Phase 3 고려**: MLX 로컬 Whisper 통합으로 3-5배 추가 속도 향상
3. **피드백 제공**: 개선 사항 제안

---

**Happy Testing! 🚀**
